<resources>
    <!-- Window/splash theme only. The UI itself is themed in Compose
         (WorkoutTheme). NoActionBar so Compose's own TopAppBar isn't doubled. -->
    <style name="Theme.GymTrainer" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
